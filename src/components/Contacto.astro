---
import "../styles/contacto.css";
import ContactInfo from "./contacto/ContactInfo.astro";
import ContactData from "./contacto/ContactData.astro";
import ContactForm from "./contacto/ContactForm.astro";
import VisitMap from "./contacto/VisitMap.astro";
---

<section class="contacto section" id="contacto">
  <div class="container">
    <div class="rule-label">
      <span class="tag">
        <svg
          width="16"
          height="16"
          viewBox="0 0 16 16"
          fill="none"
          aria-hidden="true"
        >
          <rect
            x="2"
            y="3"
            width="12"
            height="10"
            rx="1"
            stroke="currentColor"
            stroke-width="1.2"></rect>
          <polyline
            points="2,3 8,9 14,3"
            stroke="currentColor"
            stroke-width="1.2"
            fill="none"></polyline>
        </svg>
        CONTACTO
      </span>
    </div>

    <div class="contact-grid">
      <div class="contact-side">
        <ContactInfo />
        <p class="tag visit-kicker">Encuéntranos — te esperamos en el local</p>
        <VisitMap />
      </div>

      <div class="contact-main">
        <div class="contact-form-card">
          <ContactForm />
        </div>

        <div class="contact-below">
          <ContactData />
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import "../scripts/contacto";
</script>
